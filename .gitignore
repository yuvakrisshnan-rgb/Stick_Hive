node_modules/
.next/
out/
.env*
!.env.example
.dev.vars
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store
*.tsbuildinfo
playwright-report/
test-results/
/dist/
.vinext/
.wrangler/

# Sticker intake (scripts/sticker-intake.mjs): raw source images must never
# be committed. The real source folder lives outside the repo (in
# Downloads), so it can't be committed by accident either way, but these
# are a backstop in case it's ever copied in locally for convenience.
/sticker-intake-source/
/stickers-raw/

# Locally-generated product image build output
# (scripts/optimize-product-images.mjs). Derived from source images
# outside the repo; never committed.
/public/product-images-build/
