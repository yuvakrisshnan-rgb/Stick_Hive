# Decisions log

One entry per non-obvious judgment call, newest first. Only verified facts go here — see each entry's "Verified via" line.

## 2026-09-22 — Custom sticker editor: right-rail + floating-panel redesign (not pushed - local only)

**Task 0 audit, before touching anything**: the prior sprint's labeled-row toolbar (`canvas-toolbar.tsx`) plus an always-visible right column stacking Layers/My Designs/Shape/Price panels was structurally not the approved right-rail + floating-contextual-panel layout — that's the real cause, not residual icon-size drift (already fixed the prior pass). `custom-sticker/page.tsx` had no top bar/back button at all. Feature audit (grep + read, not assumed): Layers panel genuinely works; My Designs' `handleLoad` was confirmed a `console.log`-only stub against `MOCK_SAVED_DESIGNS`, doing nothing to the canvas; Add Text/Add Image/Add Emoji all genuinely wired end-to-end; align/distribute, group/ungroup, QR element, preview mode, grid toggle confirmed absent everywhere (consistent with the prior sprint's own finding). Finalize action: exactly one real path (`sticker-price-bar.tsx`'s button → `handleAddToCart` → `addCustomStickerToCart`/`updateCustomStickerDesign` by `isEditMode`) — no separate checkout/buy-now action exists, so consolidating to one top-bar button loses nothing.

**Judgment calls made from that audit, not asked about mid-task**:
- Added a floating **Image Style panel** (`image-style-panel.tsx`), symmetric with the spec's Text-style panel, even though the approved layout only names the text one. Replace/Reset Rotation/Make Die-cut Ready/Restore Original are real, working actions with no other home in the new layout — dropping them would have violated the "don't regress working functions" constraint.
- Retired `sticker-editor-shell.tsx`'s mobile bottom-sheet entirely rather than "integrating" the new flyouts into it. That sheet only ever held the old always-visible panel stack; with Layers/My Designs/Shape/Background now anchored flyouts (identical mechanism on mobile and desktop), there was no persistent-panel content left to put in a sheet. Not a guessed competing layout — the two-zone spec, taken literally, needs nothing else at any width.
- "Align" in the per-selection pill toolbar is scoped to **center the single selected element on the canvas** (both axes), not align-to-other-elements — multi-select doesn't exist (confirmed absent, same finding as the prior sprint), so relative alignment isn't buildable without a separate, larger feature.
- My Designs' "Load" stub was left non-functional (wiring a real saved-design payload into Konva layers is a separate, nontrivial task per its own code comment) but the button now reads "Coming soon" and is disabled, instead of silently doing nothing on click.
- Emoji picker (Task 4) stayed dependency-free: expanded the prior sprint's static Unicode grid into ~100 entries with keyword tags and added a real search input filtering against those tags, rather than adding an emoji-database library — genuinely searchable without a new dependency or bundle-size cost.
- Text quick-style presets ("Dark Pill", "Accent Pill") needed a per-text solid backdrop that didn't exist — added `backgroundColor` to `StickerTextLayer` and render it as a pill-shaped `Rect` behind the Konva `Text`, sized from the node's own measured height rather than a hardcoded value.
- Discovered while restructuring `sticker-canvas.tsx`: keyboard Delete/Backspace and Ctrl/Cmd+D duplicate were advertised nowhere before, but the new floating shortcuts popover (`shortcuts-popover.tsx`) needs to list real shortcuts — implemented both for real in the same keydown handler as the existing arrow-key nudge, rather than let the popover claim something false.
- The whole-sticker border's thickness was hardcoded (`STICKER_BORDER_WIDTH = 10`, no control) — lifted to real state (`borderWidth`) with a slider in the new Shape flyout.
- Added a `showCutGuide` flag distinct from the existing `showGuides` for the new grid-toggle pill: `showGuides` also gates the Transformer and is toggled off during thumbnail export, so a user-facing "grid" control could not safely reuse it without disabling the ability to resize/rotate while off.

**A layout bug found and fixed during verification, not assumed away**: with the canvas capped at a fixed 500px card, the Text/Image style panel (absolutely pinned top-right) directly overlapped the per-selection pill toolbar for the common case of a centrally-inserted text layer — confirmed via screenshot, not theoretical. Fixed by making the style panel a normal flex sibling next to the canvas card (wraps below on narrow viewports) instead of an absolute overlay on top of it, rather than fighting it with z-index/collision math. A milder version of the same collision remains when a rail flyout (e.g. Sticker Shape) is opened while a text layer stays selected — both float in the same right-hand band; the rail flyout's higher z-index keeps it fully usable on top, so nothing is actually blocked, just occasionally visually busy. Left as a known minor edge case rather than adding cross-component state to auto-close one when the other opens.

**A real regression caught by the existing test suite, not by inspection**: the rewritten text panel dropped the actual "edit what the text says" input entirely (kept only style controls) — `tests/e2e/custom-sticker.spec.ts`'s `adding text creates a selectable layer` test caught this immediately (`getByLabel(/^text$/i)` failed). Restored the labeled text-content `<input>` at the top of `text-style-panel.tsx`.

**Verified via**: `npx tsc --noEmit` clean; `eslint` on every touched/new file clean except the 3 findings confirmed pre-existing via `git diff` (two in `sticker-builder.tsx`, one in `sticker-canvas.tsx`'s untouched `useHtmlImage` hook); full local click-through via a Playwright driver script at 1440×900 and 390×844 (no `chromium-cli` available, no project run-skill existed for this repo) — top bar, 64px rail with no left sidebar, Add Text → pill toolbar + Text Style panel both visible with no overlap, Flip/Erase correctly disabled for a text layer, searchable emoji flyout, Sticker Shape flyout with 4 shapes + outline + thickness slider, shortcuts popover, zoom/grid pill, zero horizontal overflow on mobile, zero unexpected console errors (only a pre-existing dev-mode `eval()` warning and an unrelated unauthenticated `/api/auth/me` 401). Existing `tests/e2e/custom-sticker.spec.ts` 10/10 passing (chromium + mobile-safari) after the text-input fix above.

**Not pushed** — per this task's explicit instruction, local-only (`dev:vinext`) pending review.

## 2026-09-21 — Custom sticker editor: toolbar reorg + completion pass (not pushed - local only)

**Task 0 audit, before touching anything**: the task's own "already has" list claimed flip, lock, align/distribute, grid toggle, group/ungroup, QR code element, and preview mode were all already built. Checked each directly (grep across every custom-sticker file, then read the actual matches in context) rather than trusting the list:
- **Genuinely present**: image/text layers, Transformer drag/resize/rotate (real `<Transformer rotateEnabled>` in sticker-canvas.tsx), undo/redo, snap guides (real threshold-based snap logic), keyboard nudging (real `ArrowUp/Down/Left/Right` handler), zoom, die-cut contour detection, ML background removal (`@imgly/background-removal`, confirmed in package.json), duplicate. Also, unexpectedly ahead of the task's own framing: the text tool already had 6 curated fonts (fonts.ts, all from already-installed @fontsource packages) and a real color picker (native `<input type="color">`) - Task 2 turned out to need no real work.
- **Absent, confirmed by their total absence from the codebase** (every "match" was a false positive - "blocking" for "lock", Tailwind's `block`/CSS `grid` classes, Konva's own internal `<Group>` render primitive for "group", Konva's per-element text `align` prop for "align/distribute"): flip, lock, align/distribute, grid toggle, group/ungroup, QR code element.
- **Exists as code but unreachable**: `sticker-preview.tsx` (828 lines) - zero imports anywhere in the app (confirmed via grep). Its props (`imageUrl: string | null`, single-image model) predate the current multi-layer `StickerLayer[]` architecture entirely - not a missing wire-up, a genuinely incompatible data model from an earlier design.
- **The actual alignment cause**: not a layout/flex bug - icon sizes inside the *same* toolbar ranged 14-17px with no shared scale, and icon-button containers ranged size-8 to size-11 across the toolbar and side panels, each chosen per-instance.

**Scope decisions made from that audit, not asked about mid-task** (the task's own deliverable section explicitly invites "flag anything descoped with a reason," so these were decided and documented rather than blocking on a question):
- Built flip and lock for real (both explicitly listed in Task 1's secondary-tool row) since they're genuinely cheap: flip bakes a horizontal mirror into the image bitmap itself (`lib/custom-sticker/image-transform.ts`) rather than a live Konva `scaleX={-1}`, specifically because the Transformer's own `onTransformEnd` already normalizes `scaleX/scaleY` back to 1 after every resize (sticker-canvas.tsx) - fighting that would mean threading a flip sign through the drag/resize/rotate/snap math that already works and is explicitly protected by the "don't regress" constraint. Lock is a plain `locked?: boolean` field gating `draggable`/Transformer-attach/keyboard-nudge/delete - no new architecture needed.
- **Descoped align/distribute and group/ungroup**: both need multi-select (select 2+ layers at once), which doesn't exist anywhere in the current single-`selectedLayerId` architecture. Building real multi-select plus alignment/distribution math plus grouping is a genuinely large new feature, not a toolbar reorganization - didn't add non-functional buttons for these either.
- **Descoped QR code as a sticker element**: never existed despite being on the "already has"/"don't regress" list (nothing to regress); not one of the 5 numbered tasks either. The `qrcode` package in package.json is real but is used for UPI payment QR codes at checkout (`order-success/page.tsx`), unrelated to the sticker editor.
- **Descoped wiring in `sticker-preview.tsx`**: incompatible data model (see above) - would mean a rewrite, not a wire-up.

**What was built**: canvas-toolbar.tsx restructured into a labeled primary row (Add Text/Add Image/Add Emoji/Colors, Sticker.ly-style) and a secondary icon-only row on one shared size scale (17px icons, size-10 buttons throughout - the actual fix for the alignment complaint). Emoji picker (`emoji-picker.tsx`) - checked package.json first, no picker library installed; rather than add one, renders real Unicode emoji directly (every OS ships an emoji font already) and adds the selection as an ordinary text layer, so it inherits drag/resize/rotate/lock/duplicate for free instead of needing a third layer type. Sticker outline/border color (`border-color-picker.tsx` + rendering in sticker-canvas.tsx) reuses the exact boundary geometry (`buildCirclePoints`, the Square/Rounded rect params, Die-cut's `whiteBackingPoints`) the existing dashed cut-line guide already computes, as a second, solid, always-visible (not gated by `showGuides`, unlike the dashed guide) stroke - it's part of the actual sticker, not an editing aid. Manual erase (`image-eraser-modal.tsx`) - a plain HTML5 canvas (not Konva; this only needed pointer-driven pixel erasing on one static image, not the shared drag/resize/rotate/snap machinery) with a round/square brush, `globalCompositeOperation: "destination-out"`, feeding the result back through the same replace-image path that already re-triggers contour detection.

**Verified via**: full local click-through on both desktop (1280px) and mobile (iPhone 13 viewport) with real screenshots at every step - emoji added and immediately editable in the text panel, sticker border rendering as a solid orange ring around the die-cut circle, flip/lock buttons correctly enabling/disabling against real layer state (confirmed lock genuinely disables Delete), and the eraser tested against a real solid-color image generated via `sharp` (the project's existing image-processing dependency, not a synthetic/blank test fixture) - a horizontal stroke visibly punched a transparent hole through it, and contour re-detection picked up the new silhouette afterward ("✓ True die-cut outline ready"). Existing `tests/e2e/custom-sticker.spec.ts` (5 tests) still passes unmodified. Full suite: `test:unit` 13/13, full Playwright suite 73/74 with the one failure confirmed as the same pre-existing dev-server compile-contention flakiness documented earlier this session (passes cleanly in isolation with `--workers=1`), not a real regression. `next build` and `vinext build` both clean. Lint clean on every touched/new file after fixing one real issue this pass introduced (a ref read during render in the eraser's undo-button disabled state, fixed by mirroring the ref's length into a proper state variable) - the remaining lint findings in touched files were all confirmed pre-existing via `git diff` before being left alone.

**Not pushed** - per this task's explicit instruction, this is local-only (`dev:vinext`) pending review; the automated deploy workflow means a push here would go live immediately.

## 2026-09-21 — Real product images live via R2; homepage wired to D1

**R2 images (Task A)**: bucket `stickhive-product-images` created for real (R2 had just been enabled on the account) and its public dev URL enabled (`https://pub-8a6c62ba68f94cc09c8327319bffa53c.r2.dev`). `scripts/upload-product-images-r2.mjs` as originally written needed a separate R2 API token (`R2_ACCOUNT_ID`/`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`) creatable only via the Cloudflare dashboard (R2 > Manage API Tokens) - no `wrangler` CLI equivalent exists. Rather than block on that manual step, rewrote the upload path to use `wrangler r2 object put` instead (same CLOUDFLARE_API_TOKEN-authenticated wrangler session every other script in this project already shells out to for D1) - no new credential needed. `backend/storage/r2.ts`'s S3-compatible client is left in place (still the more efficient path - one AWS SDK call vs. one `wrangler` child process per file - if/when a real R2 API token exists) but is no longer what this specific script uses.

Hit the exact same Windows "space in the repo's own absolute path" bug this project already has an established fix for (`wranglerBinRelative`) - `--file=<absolute path>` under `shell:true` gets re-tokenized by cmd.exe on the space in `"stickhive 2.0"`, producing `Unknown argument: 2.0\...`. Fixed the same way: pass `--file` relative to `cwd` (repoRoot), not absolute.

Ran for real against `--db-target remote`: all 107 `status='active'` rows now have `image_url`/`thumbnail_url` pointing at the real bucket (0 failed - every active row's source image existed). Verified via `wrangler d1 execute --remote`, a direct `curl` of the R2 public URL (200), and a live Playwright check of `/shop` (all 107 `<img>` tags point at `r2.dev`, screenshot confirms real photos, not emoji).

**Homepage → D1 (Task B)**: `hero.tsx`, `trending-stickers.tsx`, `category-showcase.tsx`, `product-section.tsx`, and (unused, but fixed for consistency - confirmed via grep, no import of it exists anywhere) `bento-collection.tsx` all read the static `PRODUCTS` array directly. All are `"use client"` components, so - same constraint `store-provider.tsx` hit earlier - none of them can call `getD1()` themselves. Fixed by making `src/app/page.tsx` (previously a plain sync Server Component) `async`, fetching once via `listShopProducts()` (the same `backend/products/catalog.ts` call `/shop` makes), and threading the result down as a `products` prop through `Hero`/`ProductSection` → `TrendingStickers`/`CategoryShowcase` - one fetch, no client-side loading flicker, homepage and `/shop` structurally cannot disagree about which catalogue they're showing.

Two curation behaviors preserved as-is rather than redesigned, both now behaving differently only because the underlying data changed, not because the logic did:
- `trending-stickers.tsx`'s "trending" pick filters on `product.labels.includes("trending")` - D1 rows always have `labels: []` (no such column), so this always falls through to its existing fallback (first N in-stock products), same as it already did for the static array whenever fewer than 8 products were hand-labeled.
- `category-showcase.tsx`'s 4 hardcoded categories (Anime, Gaming, Marvel & DC, Technology) were curated against the old static array's categories. Checked directly against D1: only "Anime" has any real matches (`SELECT DISTINCT category FROM products WHERE status='active'` - Bollywood, flower stickers, Meme stickers, Rick and Morty, Stickers with dialogues, Anime; no Gaming/Marvel/DC/Technology at all) - so 3 of 4 scenes now hit the existing empty-match fallback (`allProducts.slice(index*3, index*3+3)`) instead of a real thematic match. Re-picking which 4 categories to showcase against what D1 actually has is a content/curation decision, not a data-wiring one - left alone, flagged here and in BACKLOG.md rather than decided unilaterally.

**Verified via**: local `dev:vinext` (`PRODUCTS_SOURCE=d1`) - homepage's product links are a strict subset of `/shop`'s 107 (`comm -23` against both link lists, empty output), Playwright screenshot shows real D1 products ("Rock Lee Heart Eyes", "Chubby Luffy", "Judging Cat", all category ANIME, ₹20) with zero console errors. Full pre-push gate (`next build`, `vinext build`, lint on every touched file, `test:unit` 13/13, full Playwright suite 74/74 against the *default* static-array path) also clean - confirms zero regression to the path production still uses whenever `PRODUCTS_SOURCE` is unset.

## 2026-09-21 — Checkout rewired to D1 (backend/orders/service.ts), scope expanded to the client-side cart after empirical verification failed

Asked to fix checkout's product validation (`PRODUCT_BY_ID`, a static-array `Map`, meant any D1-only product failed with "One of the selected products is unavailable."). Rewired `createOrderFromCheckout` to call `getShopProductBySlug` (`backend/products/catalog.ts` - the same flag-gated data layer `/shop` itself reads from, not a second parallel lookup), and hardened `backend/products/service.ts`'s purchasability queries to filter `status = 'active' AND needs_review = 0` explicitly (today every `needs_review=1` row happens to be `status='draft'` too - confirmed against local D1 - but the two are independent columns and the old code only checked `status`, so this closes a real gap rather than relying on that correlation holding forever).

**Scope expanded partway through, with explicit sign-off**: verifying via `dev:vinext` + a real click-through (not just direct service calls) showed the backend fix alone wasn't reachable - `src/components/shop/store-provider.tsx`'s client-side cart resolves products from the same static array too (`addToCart` does `productById.get(productId)` against a map built once from `PRODUCTS` at module load), so "Add to Cart" on a real D1 product silently no-op'd (`console.warn`'d "not found", cart stayed empty) before the request ever reached the (working) backend fix. `wishlist/page.tsx` had the identical bug for the identical reason. Flagged this to the user directly rather than unilaterally expanding scope on a 900+ line, production-live, shared provider - asked to proceed, got an explicit yes.

**Fix**: new `GET /api/products` (public, wraps `listShopProducts()` - client components can't call `getD1()` directly, no Workers binding outside the runtime). `store-provider.tsx`'s module-level `productById` became a `useState`-driven catalog: seeded synchronously from the static `PRODUCTS` array (so first paint and an already-hydrated localStorage cart behave exactly as before this change for the default/static path), then swapped for the real list once `/api/products` resolves. `getProduct(id)` exposed on the shop context so `wishlist/page.tsx` resolves against the same dynamic catalog instead of its own `PRODUCTS.filter()`.

**A real race condition found and fixed during this same verification pass, not before**: the first working version still failed on a *hard* navigation to `/checkout` (Playwright's `page.goto`, not an in-app link click) - a fresh mount reads localStorage and sanitizes it immediately, before the `/api/products` fetch has resolved, so the D1 item got silently dropped by `sanitizeCart` running against the still-static seed. Fixed with a `catalogReady` flag (set once the fetch settles, success or failure either way) that the initial-load effect now waits on before ever reading/sanitizing localStorage - a `useRef` guard makes sure that read still only happens once. Existing carts are never *retroactively* re-sanitized against a later catalog swap, only new interactions see updated data - deliberately, so nothing already in a user's cart can vanish just because the source flipped after they arrived.

**Also found, not touched**: `src/components/wishlist/wishlist-provider.tsx` (server-synced via `/api/wishlist`, real auth) turned out to be the *actual* live wishlist system - `store-provider.tsx`'s own separate `wishlist`/`toggleWishlist`/`isWishlisted` fields are dead code with zero consumers anywhere in the app (confirmed via grep), quietly duplicating the same feature against a different, also-dead localStorage key (`stickhive:wishlist` vs. the real one's `stickhive-wishlist`). Left alone per this session's standing "don't delete without flagging first" rule - noted in BACKLOG.md.

**Verified via**: a full authenticated Playwright run against `dev:vinext` with `PRODUCTS_SOURCE=d1` - sign in (debug OTP), add `eat-sleep-anime-repeat` (a real D1-only product) to cart through the actual UI, hard-navigate to `/checkout`, verify email, select UPI, place the order: `POST /api/orders` → `201`, `unitPrice: 20, lineTotal: 20` (exactly `SIZE_PRICES.Small`, computed server-side - `CheckoutItemInput`'s `"product"` variant has no price field at all, so a client-supplied price is structurally impossible, not just policy), redirected to `/order-success?orderId=...`. Then, same authenticated session, two negative cases direct against `POST /api/orders`: a `status='draft'`/`needs_review=1` product (`excuse-me-reaction`) → `400`, and a nonexistent product id → `400`, both "One of the selected products is unavailable." - same message, same path, no distinguishing signal leaked either way. Full pre-push gate (`next build`, `vinext build`, lint on every touched file, `test:unit` 13/13, the whole Playwright suite 74/74 against the *default* static-array path) also run and clean - the D1 path only activates with `PRODUCTS_SOURCE=d1`, still unset everywhere real.

**A concurrent local commit (`d477a09`, blank message) landed mid-session** - confirmed the same "both overlapping sessions are Yuva's own" situation flagged earlier this session, this time actually committing. It captured this fix's working tree exactly (verified: zero `git diff` against every substantive file it touched) but also swept in three throwaway Playwright debug scripts/screenshots this session had used for the verification above and always meant to delete before committing. Removed in `a9c1d42` rather than rewriting `d477a09` - no reset/amend, per the standing rule.

## 2026-09-21 — Category union extension: implemented and pushed (`7b79853`)

The mapping decision itself was recorded in the entry below on the same day. This entry just closes the loop: the code (product-data.ts's 6 new values, shop-catalog.tsx / category-showcase.tsx comments explaining why they're not yet in either UI list) was written, gated (`tsc --noEmit`, `next build`, `vinext build`, lint on touched files), and pushed as `7b79853`.

## 2026-09-21 — Task 3 (D1 shop rewire) found already built, uncommitted, and broken; fixed with a flag-gated fallback

Picking this session back up after a context-compaction boundary, `git status` showed 13 modified/new files - `src/app/shop/page.tsx`, `src/app/shop/[id]/page.tsx`, `backend/products/service.ts` (new), `shop-catalog.tsx`, `product-details.tsx`, `related-products.tsx`, the `/dev/product-preview` files, `next.config.ts` - all uncommitted, all implementing the D1-backed shop rewire the task list asked for. The compaction summary I'd been given said Task 3 was "Not started" - that was wrong; the working tree, not the summary, is ground truth, and I went with what `git status`/`git diff` actually showed.

**Verified via**: `git status --short` (13 modified/untracked files, none in any of the 5 commits already on the branch) and `git diff` on each (clean, well-formed - `ProductDetails`/`RelatedProducts` converted from importing the static `PRODUCTS` array to receiving `allProducts` as a prop, so both a D1 and a static call site can supply their own list).

**What was missing**: the task list's explicit requirement - "keep the static array as a flag-gated fallback until verified" - hadn't been implemented. `shop/page.tsx` called `listActiveProducts()` (D1) unconditionally, no fallback. Two concrete failures from this:
- `npm run build` failed outright: `Error: D1 binding "DB" is not configured` while prerendering `/shop` - D1 only exists at request time inside the real Workers runtime, never during `next build`'s static generation step.
- Plain `npm run dev` (no Workers runtime; `cloudflare:workers` resolves to the no-op shim) 500'd on every `/shop` request for the same reason - confirmed by curl and the dev server's own error log.

Remote D1 also isn't in a state to serve this yet regardless of local behavior: only `migrations/0006_products.sql` (schema) and the local seed (194 rows, 107 active) have run - the remote migration, the remote seed, and the R2 image upload are all still unexecuted per the standing "stop before remote D1 writes / real R2 changes" rule (see DEPLOY_CHECKLIST.md). Flipping the live shop to D1 right now, even if the build didn't fail, would serve an empty or erroring shop in production.

**Fix**: `backend/products/catalog.ts` (new) - `listShopProducts()`/`getShopProductBySlug()` wrap the D1 service and gate on `process.env.PRODUCTS_SOURCE === "d1"`, defaulting to the static array when unset. Even with the flag on, a D1 read failure is caught and falls back to static rather than 500ing the shop. `shop/page.tsx` and `shop/[id]/page.tsx` now call the wrapper and are marked `export const dynamic = "force-dynamic"` so `next build` never attempts to prerender them against D1 at all. Default (flag unset) is what ships until the remote steps in DEPLOY_CHECKLIST.md have actually run and been verified.

**Verified via**: `npm run build` (was failing on `/shop`, now completes, `/shop` and `/shop/[id]` both classified `ƒ Dynamic`), `npm run build:vinext` (completes), and a live curl of `/shop` under plain `next dev` (was 500, now 200 and renders the static array's products).

## 2026-09-21 — Bug hunt (Task 2): findings and fixes

Full `npx playwright test` run started at 27/74 failing. Two classes of cause, both real, neither the app's live-shop content:

1. **Dev-server compile contention, not app bugs.** The first couple of full-suite runs (4 parallel workers against a single `next dev` process) threw ~30s timeouts on ordinary actions (clicking "Add to Cart", `page.goto("/checkout")`). Re-running the same specs with `--workers=1` turned 14 timeouts into 5 fast (<20s), deterministic failures - confirming the timeouts were Turbopack on-demand-compile contention across concurrent workers hitting cold routes, not real waits. The e2e suite has no `webServer`/prod-build config in `playwright.config.ts`; running it against `next build && next start` instead of `next dev` would remove this class of flakiness structurally - not done here (out of scope), noted in BACKLOG.md.

2. **Real, fixed bugs** (all local, all pushed with this session's other work):
   - **CSP blocked the PIN-code lookup.** `next.config.ts`'s `connect-src` never allowlisted `api.postalpincode.in`, so the checkout form's browser-side `fetch` to it was silently CSP-blocked; `lookupPincode()`'s catch swallowed the failure into a generic "check your connection" message. Nobody's PIN code has ever auto-filled city/state in an environment where this CSP is actually enforced. Fixed by adding the host to `connect-src`. Verified live (curl to the real API returns 200; the browser fetch failed only under CSP; auto-fill test passes after the fix, fails before it).
   - **Nearly the entire static demo catalogue had broken images.** 29 of 30 static products (`src/lib/product-data.ts`) pointed at `/stickers/*.png` files that never existed in this repo (`find`/`git log --diff-filter=A` across the whole history: zero matches - not a deletion, never committed). Every `<Image>` render 400'd via Next's image optimizer. `StickerImage` (`sticker-image.tsx`) already had the right `product.image ? <Image/> : <emoji>` fallback and was unaffected once `image` was undefined; `cart-drawer.tsx`'s `CartItem`/`CustomCartItem` did not have that fallback (referenced an equally-nonexistent `/placeholder-sticker.png`) and was fixed to match the same convention. Fix: stripped the 27 dead `.png` references from `product-data.ts` (kept the 3 that reference real, existing `.svg` files - `mini-anime-pack`, `level-up`, `tiny-bot`). This is a real, pre-existing content gap, not a regression from this session - and it's exactly the gap the D1+R2 product pipeline (this session's whole body of work) exists to close. No fabricated artwork was generated to fill it.
   - **`/api/admin/orders/[orderId]/route.ts` has no GET handler** (only `PATCH`) - the test expected 401/404 on a GET, got Next's own framework-level 405. Not a leak (405 carries no data either way) and not new. Widened the expectation to include 405.
   - **Rate-limit bucket sharing across parallel tests.** Several `api-security.spec.ts` tests share the default `127.0.0.1` bucket with tests elsewhere in the same file that deliberately trip the send-otp cooldown; `fullyParallel` doesn't guarantee ordering, so a 400-expecting test could get 429 if a cooldown test ran first. Fixed the same way the file's own malformed-body tests already did: each sends its own `x-forwarded-for`.
   - **Cart drawer is (correctly, deliberately) auth-gated** - `cart-drawer.tsx`'s own comment documents this as intentional ("Your Stick Hive cart is tied to your account so only you can see it"), matching the `/orders` and `/account/settings` pattern. The old `cart-checkout.spec.ts` "Cart" tests assumed a guest could see cart contents and a checkout link directly - never true once this shipped. Rewrote them to assert the actual guest UX (sign-in prompt) instead of building a test-only auth bypass (would mean reimplementing `backend/auth/crypto.ts`'s session signing in the test harness - a real, separate test-infra gap, not solved here; see BACKLOG.md).
   - **Several `getByLabel`/`getByRole` locators were unscoped** and matched the site-wide footer's own email input/mailto link, or (for "Verify Email To Continue") a Razorpay payment-method button whose name also matched a loose `/verify email|pay/i` alternation. Added `data-testid="checkout-form"` to `customer-form.tsx`'s root section and scoped the checkout-field locators to it; the payment/verify button lives in `checkout/page.tsx` itself, outside that section, so it's matched by its own precise name instead.
   - **PIN-doesn't-exist test used `"000000"`**, which fails PIN *format* validation (`/^[1-9]\d{5}$/`) before the lookup ever runs, so it could never reach the "doesn't exist" message it was asserting for. Swapped to `"999999"` (format-valid, confirmed against the live India Post API to return `Status: "Error"`, `"No records found"`).
   - **About-page copy assertion never matched real copy.** Test expected `/from a classroom idea/i`; the page's actual text (`src/app/about/page.tsx:227`) is "began as a classroom idea" - no "from". Loosened the regex to match the real, current copy rather than editing site copy to satisfy an arbitrary test string.
   - **Homepage console-error assertion couldn't pass under dev**, full stop: `next.config.ts`'s CSP deliberately omits `'unsafe-eval'` (documented there - a real, intentional XSS-surface trade-off), and the unavoidable, dev-only, production-safe side effect is React logging `"eval() is not supported..."` on every page load (its own text: "React will never use eval() in production mode"). Filtered this one specific, known message out of the test's error collection rather than weakening the CSP or asserting against a message this suite can never keep clean under `next dev`.
   - **Mobile-only: adding text opens a bottom-sheet properties panel that covers the toolbar**, including Undo/Redo, on a mobile viewport (`sticker-editor-shell.tsx`) - a real mobile user hits the same problem. The panel's small visible "X" close button had no accessible name at all (icon-only, no `aria-label`), so it couldn't be targeted directly; added `aria-label="Close panel and deselect"` to it (and `"Minimize panel"` to its chevron sibling) and updated the test to close the panel before using Undo/Redo, matching real mobile use.

**Left as pre-existing, out of scope for this pass** (confirmed via `git diff` that none of it is on lines this session touched): `react-hooks/set-state-in-effect` errors in `shop-catalog.tsx` (`setSearch(urlSearch)` in a bare `useEffect`) and `sticker-editor-shell.tsx` (`setSheetExpanded(true)` in a bare `useEffect`); two `@typescript-eslint/no-explicit-any` errors in `customer-form.tsx` (lines 255, 292); a `no-location-assign-relative-destination` warning and two `no-unused-expressions` warnings in `cart-drawer.tsx`. Tracked in BACKLOG.md as a dedicated lint pass, per Task 2's own framing ("lint (worst files first)") rather than folded into unrelated fixes here.

**Verified via**: full `npx playwright test` (both `chromium` and `mobile-safari` projects) - 74/74 passing after all of the above; `npm run test:unit` - 13/13 passing throughout, untouched by any of this.

## 2026-09-21 — Corrected: the tsc regression in 7312d5a was real, not pre-existing

I originally told the user this was "pre-existing" (present on HEAD before my changes), based on `git stash` + `tsc --noEmit`. That test was flawed: stashing only removes *uncommitted* changes, and HEAD at the time was already `db51b12` — nine commits downstream of `7312d5a`, the commit that *created* `tests/unit/get-client-ip.test.ts`. The "clean state" I tested against already contained the broken file.

**Verified via**:
- `git log --diff-filter=A --format="%h %s" -- tests/unit/get-client-ip.test.ts` → `7312d5a Fix getClientIp() trusting a client-spoofable X-Forwarded-For` (the file's only add-commit).
- GitHub commit-statuses API (`/repos/yuvakrisshnan-rgb/Stick_Hive/commits/<sha>/status`), which is Vercel's own public record, not something I can fabricate or an injected message can stage:
  - `7312d5a`: both `stick-hive` and `stick-hive-9k1a` → `failure` ("Deployment has failed").
  - `3d0568d` (my fix commit): `stick-hive` → `success`; `stick-hive-9k1a` → `failure`, but description is "Canceled from the Vercel Dashboard" (a manual cancel, not a build error) — so `3d0568d` is the first commit where the actual build passes.
  - Every commit in between (`751394c` through `db51b12`, the whole sticker-intake run plus the products-table migration) would have carried the same broken test file and the same failure, since `npm run build`'s tsc pass covers `tests/**` via `tsconfig.json`'s `include: ["**/*.ts", ...]`.

**Root cause**: `7312d5a` added the test file and I verified `tsc --noEmit` clean *before* writing it, then never re-ran the check after — carrying forward a stale "clean" result instead of re-verifying against the file I'd just added. Fixed in `3d0568d` (readonly `NODE_ENV` cast + `allowImportingTsExtensions`).

**Standing fix, not just this incident**: from now on, every push in this session runs `npx tsc --noEmit`, `npm run build`, the vinext build, and lint on touched files first — matching what Vercel's own two projects (`stick-hive`, `stick-hive-9k1a`) actually check on every push. Never push on a failure.

## 2026-09-21 — Category union: extend with no lossy merges

Decision: add all CSV categories that don't already exactly match an existing `Category` union value, using their exact source strings — not renamed, not collapsed into a similar-sounding existing value.

- `Anime` — already an exact match, no new value needed.
- `Bollywood`, `BTS`, `flower stickers`, `Meme stickers`, `Rick and Morty`, `Stickers with dialogues` — added verbatim.

Earlier (before this was escalated to "decide it yourself, no lossy merges") I'd proposed collapsing `Rick and Morty`→`Series`, `flower stickers`→`Nature`, `Stickers with dialogues`→`Memes`, and `BTS`→a generic `K-Pop`. Explicitly overridden by this instruction: the union now has to be lossless against what's actually stored in D1/CSV, since a lossy mapping would just force a translation layer back into existence at every consumer — the opposite of the point.

**Known cosmetic side effect**: this makes the union casing/spacing inconsistent (`"Nature"` Title-Case single-word vs. `"flower stickers"` lowercase-with-space) rather than normalizing everything to one style. Deliberate — exact fidelity to the real stored strings over cosmetic uniformity.
